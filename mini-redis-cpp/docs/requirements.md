# Requirements

- Implement a simple RESP parser and serializer.
- Support basic Redis-like commands such as PING, GET, SET, DEL, EXISTS, INCR, TYPE, EXPIRE, and TTL.
- Provide a minimal event loop with epoll-based networking.
- Add tests for parser, data store, and expiry behavior.
